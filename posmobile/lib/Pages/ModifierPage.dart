import 'package:flutter/material.dart';

class ModifierPage extends StatelessWidget{
  const ModifierPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: const Text('This is the Modifier Page'),
      ),
    );
  }
}